import React, { Component } from 'react'
import { Text } from 'react-native'
import { Initializing } from '@/components'

class Home extends Component {
    render(){
        return(
            <Text>Auth</Text>
        )
    }
}

export default Home